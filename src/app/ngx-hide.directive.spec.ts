import { NgxHideDirective } from './ngx-hide.directive';

describe('NgxHideDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxHideDirective();
    expect(directive).toBeTruthy();
  });
});
